# TYPO3 documentation report - after - 2026-05-16 21:21:44

Changes applied:

- README now states TYPO3 `^14.3`, current release `14.0.0`, and
  PHPStan level max.
- `CHANGELOG.md` and `Documentation/Changelog.rst` now document the
  `14.0.0` cleanup release.
- `Documentation/guides.xml` now uses release/version `14.0.0`.

Verification:

- `validate_docs.sh` passed with 7 RST files and no common issues.
- `render_docs.sh` rendered 7 HTML/single-page/interlink documents
  successfully; generated output was removed afterward.
- `extract-all.sh --build` completed and wrote extraction data under the
  ignored `.claude/docs-extraction` cache. Its project-file extractor emits
  BSD `sed` warnings on macOS but exits successfully.
- `analyze-docs.sh` could not run on the system Bash 3.2 because the script
  uses Bash 4 associative arrays.
