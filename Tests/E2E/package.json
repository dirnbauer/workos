{
    "name": "workos-auth-e2e",
    "private": true,
    "version": "1.0.0",
    "description": "Playwright end-to-end tests for the workos_auth TYPO3 extension.",
    "scripts": {
        "test": "playwright test",
        "test:ci": "playwright test --reporter=github",
        "test:install": "playwright install --with-deps chromium"
    },
    "devDependencies": {
        "@playwright/test": "^1.63.0",
        "@types/node": "^24.0.0"
    }
}
